# ─── Required (no defaults — app refuses to boot without these) ───
DATABASE_URL=postgresql+asyncpg://travel_savings_platform:travel_savings_platform@localhost:5432/travel_savings_platform
JWT_SECRET=change-me-to-a-real-secret-at-least-32-chars

# ─── Optional (have sensible defaults) ────────────────────────────
REDIS_URL=redis://localhost:6379/0
ENVIRONMENT=local
DEBUG=true
